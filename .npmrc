# Suppress deprecated warnings during install
audit-level=moderate
fund=false

# NPM Configuration for shared hosting environments
# Disable native module compilation
rebuild-bundle=false
build-from-source=false

# Use pure JS implementations when available
optional=true

# Increase timeout for installs
fetch-timeout=300000
fetch-retries=3

# Disable native compilation
ignore-scripts=false
